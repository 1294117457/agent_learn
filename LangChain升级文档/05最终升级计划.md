# ID-AIDemo 最终升级计划（逐行版）

> **思路**：以你的 `21day` demo 为骨架，把 `ID-AIDemo/src/services/` 下的真实服务函数"嫁接"进去。
>
> 最终产物还是部署在 `ID-AIDemo` 目录里，只是在它的 `src/` 下新增一个 `agents/` 目录，把你 21day 的文件搬过去改几行。

---

## 全局视角：你的 21day 有 5 个文件，每个怎么改

```
21day/src/                          改什么                      搬到 ID-AIDemo 的哪里
─────────────────────────────────────────────────────────────────────────────────
llm.ts                    → 删掉，不搬（用 ID-AIDemo 的 aiConfig.ts 替代）
states.ts                 → 加 2 个字段                → ID-AIDemo/src/agents/states.ts
subgraphs/consultGraph.ts → 改 retrieveNode 函数体      → ID-AIDemo/src/agents/subgraphs/consultGraph.ts
subgraphs/applyGraph.ts   → 改 runCheckNode+summarize   → ID-AIDemo/src/agents/subgraphs/applyGraph.ts
subgraphs/mainGraph.ts    → 改 model 来源 + 加 export   → ID-AIDemo/src/agents/mainGraph.ts
```

加上一个新文件 `ID-AIDemo/src/routes/agent.ts`（HTTP 路由入口）。

---

## Step 0：安装依赖

在 `ID-AIDemo/` 目录下执行：

```bash
pnpm add @langchain/langgraph zod
```

> 你的 21day 项目已经用了这两个包，ID-AIDemo 还没有。`@langchain/core` 和 `@langchain/openai` ID-AIDemo 已有，不用装。

---

## Step 1：搬 `states.ts`，加 2 个字段

把 `21day/src/states.ts` 整个复制到 `ID-AIDemo/src/agents/states.ts`，然后做以下修改：

**原文（21day/src/states.ts 第 1-37 行，全文）**：

```
1  import { MessagesAnnotation, Annotation } from '@langchain/langgraph'
2
3  export const CheckTaskState = Annotation.Root({
4    documentText: Annotation<string>({ reducer: (_, x) => x, default: () => '' }),
5    checkType: Annotation<string>({ reducer: (_, x) => x, default: () => '' })
6  })
7  export type CheckTaskStateType = typeof CheckTaskState.State
8
9  // === 唯一的超级状态表 ===
10 export const MainState = Annotation.Root({
11   ...MessagesAnnotation.spec,
12
13   // 主图路由
14   intent: Annotation<'consult' | 'apply' | 'insufficient'>({ reducer: (_, x) => x, default: () => 'consult' }),
15   missingInfo: Annotation<string[]>({ reducer: (_, x) => x, default: () => [] }),
16
17   // 提取出来的核心材料文本，供 Apply 子图使用
18   documentText: Annotation<string>({ reducer: (_, x) => x, default: () => '' }),
19
20   // Apply 子图专用
21   checkResults: Annotation<string[]>({
22     reducer: (old, newVal) => [...(old ?? []), ...(newVal ?? [])],
23     default: () => []
24   }),
25
26   // Consult 子图专用
27   retrievedContext: Annotation<string>({ reducer: (_, x) => x, default: () => '' }),
28   answerDraft: Annotation<string>({ reducer: (_, x) => x, default: () => '' }),
29 })
```

**要改的地方**：在第 1 行后面加一行 import，在第 28 行后面加 2 个字段。改完后的完整文件：

```typescript
// ID-AIDemo/src/agents/states.ts

import { MessagesAnnotation, Annotation } from '@langchain/langgraph'
import type { ScoreTemplate } from '../types/scoreTemplate.js'   // ← 新增这行

export const CheckTaskState = Annotation.Root({
  documentText: Annotation<string>({ reducer: (_, x) => x, default: () => '' }),
  checkType: Annotation<string>({ reducer: (_, x) => x, default: () => '' })
})
export type CheckTaskStateType = typeof CheckTaskState.State

export const MainState = Annotation.Root({
  ...MessagesAnnotation.spec,

  intent: Annotation<'consult' | 'apply' | 'insufficient'>({ reducer: (_, x) => x, default: () => 'consult' }),
  missingInfo: Annotation<string[]>({ reducer: (_, x) => x, default: () => [] }),
  documentText: Annotation<string>({ reducer: (_, x) => x, default: () => '' }),

  checkResults: Annotation<string[]>({
    reducer: (old, newVal) => [...(old ?? []), ...(newVal ?? [])],
    default: () => []
  }),

  retrievedContext: Annotation<string>({ reducer: (_, x) => x, default: () => '' }),
  answerDraft: Annotation<string>({ reducer: (_, x) => x, default: () => '' }),

  // ↓↓↓ 以下 2 个字段是新增的 ↓↓↓
  templates: Annotation<ScoreTemplate[]>({ reducer: (_, x) => x, default: () => [] }),
  policyContext: Annotation<string>({ reducer: (_, x) => x, default: () => '' }),
})

export type MainStateType = typeof MainState.State
export const ApplyState = MainState
export type ApplyStateType = MainStateType
export const ConsultState = MainState
export type ConsultStateType = MainStateType
```

**与原文的差异汇总**：
- 第 1 行后新增：`import type { ScoreTemplate } from '../types/scoreTemplate.js'`
- 第 28 行后新增：`templates` 和 `policyContext` 两个字段
- 路径别名 `@/` 全部改为相对路径 `../`（因为 ID-AIDemo 的 tsconfig 没有配 `@` 别名）

---

## Step 2：搬 `consultGraph.ts`，替换 mock 检索

把 `21day/src/subgraphs/consultGraph.ts` 复制到 `ID-AIDemo/src/agents/subgraphs/consultGraph.ts`。

**原文（21day/src/subgraphs/consultGraph.ts 第 1-42 行，全文）**：

需要改动的有 3 处：

### 改动 ①：第 1-4 行的 import 全部替换

```
原来：
1  import {model} from '@/llm'
2  import {ConsultState,ConsultStateType} from '@/states'
3  import { HumanMessage, SystemMessage } from '@langchain/core/messages'
4  import { StateGraph,  START, END } from '@langchain/langgraph'

改为：
1  import { ChatOpenAI } from '@langchain/openai'
2  import { ConsultState, ConsultStateType } from '../states.js'
3  import { HumanMessage, SystemMessage } from '@langchain/core/messages'
4  import { StateGraph, START, END } from '@langchain/langgraph'
5  import { getEmbedding } from '../../services/embeddings.js'
6  import { searchSimilar } from '../../services/vectorStore.js'
7  import { getApiKey, getBaseUrl, getChatModel, getSystemRole } from '../../services/aiConfig.js'
```

解释：删掉 `import {model} from '@/llm'`，因为 ID-AIDemo 不用写死的 model，而是每次调用时从 `aiConfig.ts` 读最新配置来创建。新增的 3 个 import 全部来自 ID-AIDemo 已有的文件。

### 改动 ②：第 11-17 行 `retrieveNode` 函数体替换

```
原来（mock 数据）：
11 async function retrieveNode(state: ConsultStateType): Promise<Partial<ConsultStateType>> {
12   console.log("--consult:retrieveNode")
13   const userMsg = state.messages.filter(m=>m instanceof HumanMessage).at(-1)!
14     const mockContext = `政策条文：挑战杯全国一等奖 6分，二等奖 4.5分，三等奖 3分；
15   申请条件：本科在读期间获奖，颁奖机构须为教育部认定赛事。`
16   return {retrievedContext:mockContext}
17 }

改为（真实向量检索）：
async function retrieveNode(state: ConsultStateType): Promise<Partial<ConsultStateType>> {
  console.log("--consult:retrieveNode")
  const userMsg = state.messages.filter(m => m instanceof HumanMessage).at(-1)!
  const query = String(userMsg.content)

  const queryVec = await getEmbedding(query)
  const chunks = searchSimilar(queryVec, 5)

  const retrievedContext = chunks.length > 0
    ? chunks.map((c, i) => `[${i + 1}] ${c.content}`).join('\n\n')
    : '（知识库暂无相关内容）'

  return { retrievedContext }
}
```

这 4 行核心代码从哪来？从 `ID-AIDemo/src/services/ragChain.ts` 第 45-51 行的 `buildContext()` 函数：

```
45 async function buildContext(userMessage: string): Promise<string> {
46   const queryVec = await getEmbedding(userMessage)          ← 搬这行
47   const chunks = searchSimilar(queryVec, 5)                 ← 搬这行
48   return chunks.length > 0
49     ? chunks.map((c, i) => `[${i + 1}] ${c.content}`).join('\n\n')   ← 搬这行
50     : '（知识库暂无相关内容）'                                          ← 搬这行
51 }
```

### 改动 ③：第 24-33 行 `answerNode` 里的 `model` 改为动态创建

```
原来：
32   const reply = await model.invoke(messages)

改为：
  const model = new ChatOpenAI({
    apiKey: getApiKey(),
    configuration: { baseURL: getBaseUrl() },
    modelName: getChatModel(),
    temperature: 0.3,
  })
  const reply = await model.invoke(messages)
```

这个 `new ChatOpenAI(...)` 从哪来？从 `ID-AIDemo/src/services/ragChain.ts` 第 8-15 行的 `createChatModel()`：

```
8  function createChatModel() {
9    return new ChatOpenAI({
10     apiKey: getApiKey(),
11     configuration: { baseURL: getBaseUrl() },
12     modelName: getChatModel(),
13     temperature: 0.3,
14   })
15 }
```

同时把 `answerNode` 的 SystemMessage 改为使用 `getSystemRole()`：

```
原来（第 29 行）：
  new SystemMessage(`你是政策咨询助手。根据以下政策条文回答用户问题：\n${state.retrievedContext}`)

改为：
  new SystemMessage(`${getSystemRole()}\n\n【知识库检索结果】\n${state.retrievedContext}`)
```

`getSystemRole()` 来自 `ID-AIDemo/src/services/aiConfig.ts` 第 21-23 行，它从 SQLite 读系统角色配置。

### 改完后的完整文件

```typescript
// ID-AIDemo/src/agents/subgraphs/consultGraph.ts

import { ChatOpenAI } from '@langchain/openai'
import { ConsultState, ConsultStateType } from '../states.js'
import { HumanMessage, SystemMessage } from '@langchain/core/messages'
import { StateGraph, START, END } from '@langchain/langgraph'
import { getEmbedding } from '../../services/embeddings.js'
import { searchSimilar } from '../../services/vectorStore.js'
import { getApiKey, getBaseUrl, getChatModel, getSystemRole } from '../../services/aiConfig.js'

async function retrieveNode(state: ConsultStateType): Promise<Partial<ConsultStateType>> {
  console.log("--consult:retrieveNode")
  const userMsg = state.messages.filter(m => m instanceof HumanMessage).at(-1)!
  const query = String(userMsg.content)

  const queryVec = await getEmbedding(query)
  const chunks = searchSimilar(queryVec, 5)

  const retrievedContext = chunks.length > 0
    ? chunks.map((c, i) => `[${i + 1}] ${c.content}`).join('\n\n')
    : '（知识库暂无相关内容）'

  return { retrievedContext }
}

async function answerNode(state: ConsultStateType): Promise<Partial<ConsultStateType>> {
  console.log("--consult:answerNode")
  const userMsg = state.messages.filter(m => m instanceof HumanMessage).at(-1)!
  const model = new ChatOpenAI({
    apiKey: getApiKey(),
    configuration: { baseURL: getBaseUrl() },
    modelName: getChatModel(),
    temperature: 0.3,
  })
  const messages = [
    new SystemMessage(`${getSystemRole()}\n\n【知识库检索结果】\n${state.retrievedContext}`),
    new HumanMessage(String(userMsg.content))
  ]
  const reply = await model.invoke(messages)
  return { answerDraft: String(reply.content), messages: [reply] }
}

export const consultSubgraph = new StateGraph(ConsultState)
  .addNode('retrieve', retrieveNode)
  .addNode('answer', answerNode)
  .addEdge(START, 'retrieve')
  .addEdge('retrieve', 'answer')
  .addEdge('answer', END)
  .compile()
```

---

## Step 3：搬 `applyGraph.ts`，替换 mock 检查

把 `21day/src/subgraphs/applyGraph.ts` 复制到 `ID-AIDemo/src/agents/subgraphs/applyGraph.ts`。

你的 demo 用 `Send` 并行执行 3 个 mock 检查，但真实的证书分析流程是有依赖关系的（必须先检索政策，才能对照模板匹配），所以这里改为顺序流程，不再用 `Send`。

### 整体结构变化

```
demo 结构（并行 mock）：                    改后结构（顺序真实）：
START → dispatch → Send(3个check) →       START → fetchPolicy → analyzeAndMatch →
        runCheck → summarize → END                 summarize → END
```

### 改完后的完整文件

```typescript
// ID-AIDemo/src/agents/subgraphs/applyGraph.ts

import { ChatOpenAI } from '@langchain/openai'
import { ApplyState, ApplyStateType } from '../states.js'
import { StateGraph, START, END } from '@langchain/langgraph'
import { HumanMessage, SystemMessage } from '@langchain/core/messages'
import { z } from 'zod'
import { getEmbedding } from '../../services/embeddings.js'
import { searchSimilar } from '../../services/vectorStore.js'
import { getApiKey, getBaseUrl, getChatModel } from '../../services/aiConfig.js'

// ─── 节点1：检索相关政策 ───
// 逻辑搬自 ID-AIDemo/src/services/analyzeChain.ts 第 68-74 行
async function fetchPolicyNode(state: ApplyStateType): Promise<Partial<ApplyStateType>> {
  console.log("--apply:fetchPolicy")
  const queryVec = await getEmbedding(state.documentText.slice(0, 512))
  const chunks = searchSimilar(queryVec, 5)
  const policyContext = chunks.length > 0
    ? chunks.map((c, i) => `[${i + 1}] ${c.content}`).join('\n\n')
    : '（知识库暂无相关政策）'
  return { policyContext }
}

// ─── 节点2：模板匹配分析 ───
// prompt 搬自 ID-AIDemo/src/services/analyzeChain.ts 第 85-112 行
// 但用 withStructuredOutput 替代旧的手动 JSON.parse
const SuggestionSchema = z.object({
  suggestions: z.array(z.object({
    templateId: z.number(),
    templateName: z.string(),
    ruleId: z.number(),
    ruleName: z.string(),
    estimatedScore: z.number(),
    reason: z.string().describe('一句话匹配理由，不超过50字'),
  }))
})

async function analyzeAndMatchNode(state: ApplyStateType): Promise<Partial<ApplyStateType>> {
  console.log("--apply:analyzeAndMatch")

  // 如果没有模板数据，直接跳过
  if (!state.templates || state.templates.length === 0) {
    return { checkResults: ['{"error":"无可用加分模板"}'] }
  }

  // 精简模板结构，减少 token 消耗
  // 搬自 ID-AIDemo/src/services/analyzeChain.ts 第 77-82 行
  const templatesForPrompt = state.templates.map(t => ({
    id: t.id,
    templateName: t.templateName,
    templateType: t.templateType,
    rules: t.rules.map(r => ({ id: r.id, ruleName: r.ruleName, ruleScore: r.ruleScore }))
  }))

  const model = new ChatOpenAI({
    apiKey: getApiKey(),
    configuration: { baseURL: getBaseUrl() },
    modelName: getChatModel(),
    temperature: 0.1,
  }).withStructuredOutput(SuggestionSchema)

  // prompt 搬自 ID-AIDemo/src/services/analyzeChain.ts 第 89-112 行
  // 但去掉了"只输出JSON"的指令（因为 withStructuredOutput 自动处理）
  const result = await model.invoke([
    new SystemMessage('你是厦门大学信息学院推免加分审核专家。分析证明材料，判断可以申请哪些加分项。如果无匹配返回空数组。'),
    new HumanMessage(`学生上传了以下证明材料：
---
${state.documentText.slice(0, 2000)}
---

可申请的加分模板列表：
${JSON.stringify(templatesForPrompt, null, 2)}

相关加分政策参考：
${state.policyContext}

请分析证明材料，判断学生可以申请哪些加分项。`),
  ])

  return { checkResults: result.suggestions.map(s => JSON.stringify(s)) }
}

// ─── 节点3：汇总回复 ───
// 保留你 demo 的 summarizeNode 思路，但输出更实用的格式
async function summarizeNode(state: ApplyStateType): Promise<Partial<ApplyStateType>> {
  console.log("--apply:summarize")

  // 尝试解析 checkResults 里的 JSON
  const suggestions = state.checkResults
    .map(r => { try { return JSON.parse(r) } catch { return null } })
    .filter(Boolean)

  if (suggestions.length === 0 || suggestions[0]?.error) {
    return { messages: [new (await import('@langchain/core/messages')).AIMessage(
      '根据您提供的材料，暂未匹配到符合条件的加分项。请确认材料内容是否完整，或补充更多信息。'
    )] }
  }

  const summary = suggestions.map((s: any) =>
    `• **${s.templateName}** / ${s.ruleName}\n  预计加分：${s.estimatedScore} 分\n  理由：${s.reason}`
  ).join('\n\n')

  const { AIMessage } = await import('@langchain/core/messages')
  return { messages: [new AIMessage(`为您匹配到以下加分项：\n\n${summary}`)] }
}

// ─── 图结构 ───
export const applySubgraph = new StateGraph(ApplyState)
  .addNode('fetchPolicy', fetchPolicyNode)
  .addNode('analyzeAndMatch', analyzeAndMatchNode)
  .addNode('summarize', summarizeNode)
  .addEdge(START, 'fetchPolicy')
  .addEdge('fetchPolicy', 'analyzeAndMatch')
  .addEdge('analyzeAndMatch', 'summarize')
  .addEdge('summarize', END)
  .compile()
```

**与 demo 的区别汇总**：

| demo 的什么 | 改成了什么 | 代码从哪来 |
|---|---|---|
| `import { model } from "@/llm"` | `import { ChatOpenAI }` + `new ChatOpenAI(...)` | `ragChain.ts` 第 8-15 行 |
| `import ... from '@/states'` | `import ... from '../states.js'` | 路径调整 |
| `dispatchNode` + `dispatchRoute` + `Send` | 删除，改为顺序 `addEdge` | — |
| `runCheckNode` 里 3 个 mock prompt | `fetchPolicyNode` 做真实向量检索 | `analyzeChain.ts` 第 68-74 行 |
| — | `analyzeAndMatchNode` 做模板匹配 | `analyzeChain.ts` 第 77-112 行 |
| `summarizeNode` 简单总结 | `summarizeNode` 格式化匹配结果 | 新写 |

---

## Step 4：搬 `mainGraph.ts`，改 model 来源 + 加 export 接口

把 `21day/src/subgraphs/mainGraph.ts` 复制到 `ID-AIDemo/src/agents/mainGraph.ts`。

### 改动 ①：第 1-9 行 import 替换

```
原来：
1  import {model} from '@/llm'
2  import {MainState, MainStateType} from '@/states'
3  import { applySubgraph } from './applyGraph'
4  import { consultSubgraph } from './consultGraph'
5  import { StateGraph, START, END, interrupt } from '@langchain/langgraph'
6  import { HumanMessage, AIMessage } from '@langchain/core/messages'
7  import { z } from 'zod'
8  import { RunnableConfig } from '@langchain/core/runnables'
9  import { MemorySaver,Command} from '@langchain/langgraph'

改为：
import { ChatOpenAI } from '@langchain/openai'
import { MainState, MainStateType } from './states.js'
import { applySubgraph } from './subgraphs/applyGraph.js'
import { consultSubgraph } from './subgraphs/consultGraph.js'
import { StateGraph, START, END, interrupt } from '@langchain/langgraph'
import { HumanMessage, AIMessage } from '@langchain/core/messages'
import { z } from 'zod'
import { MemorySaver, Command } from '@langchain/langgraph'
import { getApiKey, getBaseUrl, getChatModel } from '../services/aiConfig.js'
import type { ScoreTemplate } from '../types/scoreTemplate.js'
```

### 改动 ②：第 13-18 行删掉写死的 config

```
删掉：
13 const config:RunnableConfig={
14   configurable: {
15     thread_id: 'test-thread21',
16     use_id: 'zch'
17   }
18 }
```

这个 config 改为由路由层传入（Step 5 会处理）。

### 改动 ③：第 20-26 行 `classifierModel` 改为函数内创建

```
原来（写死的全局变量）：
20 const classifierModel = model.withStructuredOutput(...)

改为（写在 classifyNode 函数内部）：
在 classifyNode 函数内部创建，见下面完整代码
```

### 改动 ④：第 77-93 行的图结构保持不变，只改 export 方式

图结构完全不变。删掉文件末尾注释掉的测试代码（第 95-110 行），新增 export 接口函数。

### 改完后的完整文件

```typescript
// ID-AIDemo/src/agents/mainGraph.ts

import { ChatOpenAI } from '@langchain/openai'
import { MainState, MainStateType } from './states.js'
import { applySubgraph } from './subgraphs/applyGraph.js'
import { consultSubgraph } from './subgraphs/consultGraph.js'
import { StateGraph, START, END, interrupt } from '@langchain/langgraph'
import { HumanMessage, AIMessage } from '@langchain/core/messages'
import { z } from 'zod'
import { MemorySaver, Command } from '@langchain/langgraph'
import { getApiKey, getBaseUrl, getChatModel } from '../services/aiConfig.js'
import type { ScoreTemplate } from '../types/scoreTemplate.js'

const checkpointer = new MemorySaver()

// ─── classifyNode ───
// 搬自你 demo 的 mainGraph.ts 第 28-59 行，只改 model 来源
async function classifyNode(state: MainStateType): Promise<Partial<MainStateType>> {
  // 原来的 classifierModel 是全局变量，现在改为函数内创建
  const classifierModel = new ChatOpenAI({
    apiKey: getApiKey(),
    configuration: { baseURL: getBaseUrl() },
    modelName: getChatModel(),
    temperature: 0,                    // demo 里是 0.2，分类任务用 0 更稳定
  }).withStructuredOutput(
    z.object({
      intent: z.enum(['consult', 'apply', 'insufficient']).describe("如果是咨询政策为consult；如果想要申请加分但欠缺赛事名称/时间/等级信息为insufficient；如果要申请且信息完整为apply"),
      missing: z.array(z.string()).describe("只有在 intent 为 insufficient 时，列出缺失的字段"),
      documentText: z.string().describe("只有在 intent 为 apply 时，提取用户用来申请的完整材料原文")
    })
  )

  // ↓↓↓ 以下和你 demo 第 29-59 行完全一样，一字不改 ↓↓↓
  const allUserText = state.messages
    .filter(m => m instanceof HumanMessage)
    .map(m => m.content)
    .join('\n')

  const messages = [new HumanMessage(`
    分析以下用户的多轮输入，判断意图并提取信息：
    
    【分类与校验规则】：
    1. 如果用户是单纯询问政策或了解相关信息（如"挑战杯能加多少分"），意图记为 consult。
    2. 如果用户表达了"想申请加分"或正在提交比赛凭证，必须严格校验其提供的信息是否完整！
       完整材料必须同时包含以下4个关键要素：【赛事名称】、【奖项等级】、【时间】、【申请人角色】。
       * 特别注意：赛事名称允许使用简称（如"挑战杯"、"互联网+"、"国创"等），只要提到了即可，不要强求全称。
    3. 如果是申请意图，但上述4个要素有任何缺失，意图必须记为 insufficient，并在 missing 数组中准确列出还缺少的要素名称。
    4. 如果是申请意图，且上述4个要素全部齐备，意图记为 apply，并将所有用于申请的原始材料文本合并提取到 documentText 中。
    
    【用户输入历史】：
    ${allUserText}
  `)]

  const reply = await classifierModel.invoke(messages)

  console.log(`-main:classifyNode: 当前收集信息: ${allUserText.replace(/\n/g, ' ')}`)
  console.log(`-main:classifyNode: 意图: ${reply.intent}, 缺失: ${reply.missing}, 材料提取: ${reply.documentText?.slice(0, 10)}...`)

  return {
    intent: reply.intent,
    missingInfo: reply.missing || [],
    documentText: reply.documentText || ''
  }
}

// ─── askForMoreNode ───
// 搬自你 demo 的 mainGraph.ts 第 64-75 行，一字不改
async function askForMoreNode(state: MainStateType): Promise<Partial<MainStateType>> {
  const question = `申请材料不完整，还缺：${state.missingInfo.join('、')}。请补充：`
  console.log(`-main:askForMoreNode: ${question}`)

  const userAnswer = interrupt(question)
  return {
    messages: [
      new AIMessage(question),
      new HumanMessage(String(userAnswer))
    ]
  }
}

// ─── 图结构 ───
// 搬自你 demo 的 mainGraph.ts 第 77-93 行，完全一样
const mainGraph = new StateGraph(MainState)
  .addNode('classify', classifyNode)
  .addNode('ask', askForMoreNode)
  .addNode('applyGraph', applySubgraph)
  .addNode('consultGraph', consultSubgraph)

  .addEdge(START, 'classify')
  .addConditionalEdges('classify', (s) => s.intent, {
    insufficient: 'ask',
    apply: 'applyGraph',
    consult: 'consultGraph'
  })
  .addEdge('ask', 'classify')
  .addEdge('applyGraph', END)
  .addEdge('consultGraph', END)

const app = mainGraph.compile({ checkpointer })

// ─── 以下是新增的 export 接口，供 HTTP 路由调用 ───

export interface AgentInput {
  userInput: string
  documentText?: string
  templates?: ScoreTemplate[]
  sessionId: string
}

/**
 * 普通调用（非流式）
 * 路由层调用这个函数就行
 */
export async function invokeAgent(input: AgentInput) {
  const config = { configurable: { thread_id: input.sessionId } }

  const result = await app.invoke({
    messages: [new HumanMessage(input.userInput)],
    documentText: input.documentText ?? '',
    templates: input.templates ?? [],
  }, config)

  const lastAI = result.messages
    .filter((m: any) => m._getType() === 'ai')
    .at(-1)

  return {
    reply: String(lastAI?.content ?? ''),
    intent: result.intent,
    suggestions: result.checkResults
      .map((r: string) => { try { return JSON.parse(r) } catch { return null } })
      .filter(Boolean),
  }
}

/**
 * interrupt 恢复调用
 * 用户补充信息后，前端调用 /agent/resume，路由层调用这个函数
 */
export async function resumeAgent(sessionId: string, supplement: string) {
  const config = { configurable: { thread_id: sessionId } }
  const result = await app.invoke(
    new Command({ resume: supplement }),
    config
  )
  const lastAI = result.messages
    .filter((m: any) => m._getType() === 'ai')
    .at(-1)
  return {
    reply: String(lastAI?.content ?? ''),
    intent: result.intent,
  }
}
```

**与 demo 的差异汇总**：

| demo 行号 | 改了什么 | 为什么改 |
|---|---|---|
| 第 1 行 `import {model} from '@/llm'` | 删掉，改为 `import { ChatOpenAI }` + `import ... aiConfig` | 用 ID-AIDemo 的动态配置 |
| 第 2-4 行 import 路径 | `@/xxx` → `./xxx.js` 或 `../xxx.js` | ID-AIDemo 无 `@` 别名 |
| 第 13-18 行 写死的 config | 删掉 | 改由路由层传入 sessionId |
| 第 20-26 行 全局 classifierModel | 移到 classifyNode 内部 | 每次读最新的 aiConfig |
| 第 28-59 行 classifyNode 函数体 | **一字不改** | — |
| 第 64-75 行 askForMoreNode 函数体 | **一字不改** | — |
| 第 77-93 行 图结构 | **完全一样** | — |
| 第 95-110 行 注释掉的测试代码 | 删掉，换成 export 接口函数 | 供路由调用 |

---

## Step 5：新建 `src/routes/agent.ts`（HTTP 路由入口）

这是唯一一个完全新写的文件。它的工作是：接收 HTTP 请求 → 调用 `invokeAgent()` / `resumeAgent()` → 返回 JSON。

参照 `ID-AIDemo/src/routes/chat.ts`（15 行 + multer 文件上传参照 `analyze.ts`）的写法：

```typescript
// ID-AIDemo/src/routes/agent.ts（新建）

import { Router } from 'express'
import multer from 'multer'
import path from 'path'
import { fileURLToPath } from 'url'
import fs from 'fs'
import { parseFile } from '../services/docParser.js'
import { invokeAgent, resumeAgent } from '../agents/mainGraph.js'
import { fetchTemplatesFromDb } from '../db/mysql.js'
import type { ScoreTemplate } from '../types/scoreTemplate.js'

const __dirname = path.dirname(fileURLToPath(import.meta.url))
const UPLOAD_DIR = path.resolve(__dirname, '../../uploads')
fs.mkdirSync(UPLOAD_DIR, { recursive: true })

// multer 配置搬自 ID-AIDemo/src/routes/analyze.ts 第 18-28 行
// 但放宽了文件类型限制（不止 PDF）
const upload = multer({
  dest: UPLOAD_DIR,
  limits: { fileSize: 10 * 1024 * 1024 },
})

const router = Router()

/**
 * POST /agent/chat
 *
 * 统一入口：支持纯文字 + 可选文件上传
 *
 * Body (multipart/form-data):
 *   message   : string   用户输入的文字
 *   sessionId : string   会话ID（前端维护，同一次对话不变）
 *   file?     : File     可选，上传 PDF/DOCX 等材料
 *   templates?: string   可选，JSON 字符串的 ScoreTemplate[]
 */
router.post('/chat', upload.single('file'), async (req, res) => {
  const { message = '', sessionId = 'default' } = req.body as {
    message?: string
    sessionId?: string
  }

  if (!message?.trim() && !req.file) {
    res.json({ code: 400, msg: '请输入文字或上传文件', data: null })
    return
  }

  // 1. 解析上传文件（如果有）
  //    逻辑搬自 ID-AIDemo/src/routes/analyze.ts 第 47-60 行
  let documentText = ''
  if (req.file) {
    const originalName = Buffer.from(req.file.originalname, 'latin1').toString('utf8')
    const hintExt = path.extname(originalName).toLowerCase()
    documentText = await parseFile(req.file.path, hintExt)
    fs.unlink(req.file.path, () => {})
  }

  // 2. 解析模板数据（如果有）
  //    逻辑搬自 ID-AIDemo/src/routes/analyze.ts 第 63-72 行
  let templates: ScoreTemplate[] = []
  const templatesRaw = (req.body as { templates?: string }).templates
  if (templatesRaw) {
    try {
      const parsed: unknown = JSON.parse(templatesRaw)
      templates = Array.isArray(parsed) ? (parsed as ScoreTemplate[]) : []
    } catch { /* ignore */ }
  }
  // 没有传 templates 就从 MySQL 查
  if (templates.length === 0) {
    templates = await fetchTemplatesFromDb()
  }

  // 3. 调用 LangGraph Agent
  try {
    const result = await invokeAgent({
      userInput: message.trim(),
      documentText,
      templates,
      sessionId,
    })
    res.json({ code: 200, msg: '成功', data: result })
  } catch (err: any) {
    // interrupt 会导致 GraphInterrupt，需要特殊处理返回给前端
    if (err?.interrupts?.length > 0 || err?.name === 'GraphInterrupt') {
      const question = err.interrupts?.[0]?.value ?? String(err)
      res.json({
        code: 200,
        msg: '需要补充信息',
        data: { interrupted: true, question }
      })
    } else {
      console.error('[agent/chat] error:', err)
      res.json({ code: 500, msg: `处理失败: ${String(err)}`, data: null })
    }
  }
})

/**
 * POST /agent/resume
 *
 * 用户补充信息后恢复 interrupt
 *
 * Body (application/json):
 *   sessionId  : string   必须和之前的 /agent/chat 一致
 *   supplement : string   用户补充的信息
 */
router.post('/resume', async (req, res) => {
  const { sessionId, supplement } = req.body as {
    sessionId?: string
    supplement?: string
  }

  if (!sessionId || !supplement?.trim()) {
    res.json({ code: 400, msg: '缺少 sessionId 或 supplement', data: null })
    return
  }

  try {
    const result = await resumeAgent(sessionId, supplement.trim())
    res.json({ code: 200, msg: '成功', data: result })
  } catch (err) {
    console.error('[agent/resume] error:', err)
    res.json({ code: 500, msg: `恢复失败: ${String(err)}`, data: null })
  }
})

export default router
```

**每段代码的来源**：

| 行 | 从哪来 |
|---|---|
| multer 配置 | `ID-AIDemo/src/routes/analyze.ts` 第 18-28 行 |
| 文件解析 `parseFile(...)` | `ID-AIDemo/src/routes/analyze.ts` 第 54-59 行 |
| 模板解析 `JSON.parse(templatesRaw)` | `ID-AIDemo/src/routes/analyze.ts` 第 63-72 行 |
| `fetchTemplatesFromDb()` | `ID-AIDemo/src/db/mysql.ts` 第 25 行（已有函数，直接调用） |
| `invokeAgent()` / `resumeAgent()` | Step 4 新增的 export 函数 |
| 路由风格（`res.json({ code, msg, data })`) | `ID-AIDemo/src/routes/chat.ts` 统一风格 |

---

## Step 6：在 `src/index.ts` 挂载新路由

**原文（ID-AIDemo/src/index.ts 第 6-9 行）**：

```
6  import chatRouter from './routes/chat.js'
7  import knowledgeRouter from './routes/knowledge.js'
8  import analyzeRouter from './routes/analyze.js'
9  import configRouter from './routes/config.js'
```

**在第 9 行后面加一行**：

```typescript
import agentRouter from './routes/agent.js'
```

**原文（第 20-23 行）**：

```
20 app.use('/chat', chatRouter)
21 app.use('/knowledge', knowledgeRouter)
22 app.use('/analyze', analyzeRouter)
23 app.use('/config', configRouter)
```

**在第 23 行后面加一行**：

```typescript
app.use('/agent', agentRouter)
```

**原文（第 33-43 行接口列表）**：

在 `console.log('  PUT  /config')` 后面加两行：

```typescript
    console.log('  POST /agent/chat')
    console.log('  POST /agent/resume')
```

其他所有代码不动。

---

## 最终的文件结构

```
ID-AIDemo/src/
├── agents/                           ← 整个目录是新增的
│   ├── states.ts                     ← 来自 21day/src/states.ts，加了 2 个字段
│   ├── mainGraph.ts                  ← 来自 21day/src/subgraphs/mainGraph.ts，改了 model 来源
│   └── subgraphs/
│       ├── consultGraph.ts           ← 来自 21day/src/subgraphs/consultGraph.ts，mock→真实检索
│       └── applyGraph.ts             ← 来自 21day/src/subgraphs/applyGraph.ts，mock→真实匹配
├── routes/
│   ├── agent.ts                      ← 新建，HTTP 入口
│   ├── chat.ts                       ← 不动（旧路由保留）
│   ├── analyze.ts                    ← 不动（旧路由保留）
│   ├── config.ts                     ← 不动
│   └── knowledge.ts                  ← 不动
├── services/                         ← 全部不动
│   ├── aiConfig.ts
│   ├── embeddings.ts
│   ├── vectorStore.ts
│   ├── docParser.ts
│   ├── ragChain.ts                   ← 保留，旧 /chat/send 仍在用
│   └── analyzeChain.ts               ← 保留，旧 /analyze 仍在用
├── db/                               ← 全部不动
├── seed/                             ← 全部不动
├── types/                            ← 全部不动
└── index.ts                          ← 只加了 2 行（import + use）
```

---

## 每步工作量和测试方式

| 步骤 | 做什么 | 实际改动量 | 测试方式 |
|------|--------|-----------|---------|
| Step 0 | `pnpm add @langchain/langgraph zod` | 1 条命令 | `pnpm list @langchain/langgraph` |
| Step 1 | 搬 states.ts，加 2 字段 | 复制 + 加 3 行 | `npx tsc --noEmit` 编译通过 |
| Step 2 | 搬 consultGraph.ts，改 retrieveNode | 改 import + 改 1 个函数体 | 写个 test.ts 单独 invoke 子图 |
| Step 3 | 重写 applyGraph.ts | 改动最大，约 80 行 | 写个 test.ts 单独 invoke 子图 |
| Step 4 | 搬 mainGraph.ts，改 model + 加 export | 改 import + 移动 classifierModel + 加 40 行 | 写个 test.ts 调用 invokeAgent |
| Step 5 | 新建 agent.ts 路由 | 约 100 行新代码 | `curl -X POST http://localhost:3001/agent/chat` |
| Step 6 | index.ts 加 2 行 | 加 2 行 | `pnpm dev` 启动看接口列表 |

---

## 测试命令

启动后测试三种场景：

```bash
# 场景1：政策咨询
curl -X POST http://localhost:3001/agent/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"挑战杯二等奖能加多少分？","sessionId":"test-1"}'

# 场景2：信息不足 → 触发 interrupt
curl -X POST http://localhost:3001/agent/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"我想申请加分","sessionId":"test-2"}'

# 场景2续：补充信息 → resume
curl -X POST http://localhost:3001/agent/resume \
  -H "Content-Type: application/json" \
  -d '{"sessionId":"test-2","supplement":"我是队长，2023年9月获得挑战杯全国一等奖"}'

# 场景3：上传文件
curl -X POST http://localhost:3001/agent/chat \
  -F "message=帮我分析这份证书" \
  -F "sessionId=test-3" \
  -F "file=@./test.pdf"
```
